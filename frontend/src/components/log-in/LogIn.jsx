import React from "react";
import { Button } from "../button/Button.styles";

const LogIn = () => {
  return (
    <div>
      <Button>Log Sign in with Google</Button>
    </div>
  );
};

export default LogIn;
